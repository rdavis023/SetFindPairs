#ifndef FIND_PAIRS_H
#define FIND_PAIRS_H

#include <vector>
#include <unordered_set>

std::vector<std::vector<int>> findPairs(
    const std::vector<int>& arr1,
    const std::vector<int>& arr2,
    int target
);

#endif // FIND_PAIRS_H